// Ejemplo de uso de un bucle for para iterar sobre un array
// Este código crea un array de hardware y utiliza un bucle for para imprimir cada elemento en la consola.

let array_hardware = ["mouse", "teclado", "monitor", "disco duro", "tarjeta madre"];

for (let i = 0; i < array_hardware.length; i++) {
    console.log(array_hardware[i]); // Imprime cada elemento del array en la consola
}

